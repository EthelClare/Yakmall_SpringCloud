spring:
  application:
    name: gateway-service
  profiles:
    active: new
  cloud:
    nacos:
      server-addr: 192.168.64.9
      config:
        file-extension: yaml
        shared-configs:
          - dataId: shared-log.yaml # 共享日志配置